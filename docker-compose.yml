version: '3'

services:
   places-app:
    container_name: places
    image: astb01/places-app:latest
    ports:
      - '5001:80'

# Build image for places app
# docker build . -t places-app:v2

# To look for images
# docker images

# To run the new image
# docker run -p 5000:80 -d places-app:v2
